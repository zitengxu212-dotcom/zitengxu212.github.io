# 开发日志 — 2026-06-09

## 完成事项

### 1. 详情页画廊重构
- **删除水平滑动 + 左右箭头**：移除 16 个 gallery-arrow 按钮及 SVG，清理 `galleryShift` JS 逻辑（~45 行），CSS 简化
- **改为垂直堆叠 + 渐出动效**：图片纵向排列，width:100% + height:auto 保原比例，无裁剪；IntersectionObserver 提前 100px 触发 fade-in + translateY 上浮动效（0.6s ease-out）
- 提交 `58aa622`、`9578b28`

### 2. Work 区 Graphic Design 从 4 项目扩展到 6 项目
- `projects` 注册表新增 09（Photo/Art）、10（Type/Layout），均为 graphic 分类
- DOM 卡片从 8 张扩展到 12 张，`renderCards` 改造支持可变数量（按需 show/hide）
- `index.html` 追加 4 张 `.t-card` + `#detail-09`、`#detail-10` 两个 article
- 提交 `b136b04`

### 3. 详情页交互增强
- **单击背景关闭**：`!e.target.closest('.project-detail')` → 点击黑色区域返回 Work 区
- **顶栏导航联动**：详情面板打开时点击 Home/About/Works → 先 closeDetail() → 等 450ms 动画 → scrollIntoView
- **打开时复位顶部**：`panel.scrollTop = 0`（放在 classList.add('open') 之后，确保可见才能设置）
- Client → Award 文案替换（8 处）
- 提交 `2871c3f`、`b136b04`、`234c0e9`

### 4. 全部项目图片替换
- 01-10 共 10 个项目的卡片图（`_cardImages`）和详情画廊图（`_galleryImages`）全部从 placehold.co 占位图替换为真实图片（`主页图片/0X/` 目录）
- 01-04 先替换，05-10 后续批次完成
- 图片 1 同时用于 Work 卡片封面和详情页画廊第一张（同路径双引用，不复制文件）
- 提交 `b136b04`

### 5. 详情页样式调整
- 边缘间距：5px → 50px（三个断点统一），去除 `max-width` 使内容撑满视窗
- 英文正文字体：从 `var(--font-primary)` 改为系统默认字体栈（`-apple-system, BlinkMacSystemFont, 'Segoe UI'...`）
- EN 段落 `text-align: justify` 两端对齐
- 提交 `234c0e9`

## 待办事项
- 详情页内容管理系统化（md 文档 → HTML 同步方案）
- 图片压缩至 ≤200KB
- 轮播卡片 hover 状态卡住 bug 修复
- 05-10 详情页真实文案填写

## 备注
- Gallery 箭头完全删除后，用户反馈需恢复滚动查看多图的能力 → 改为垂直堆叠方案
- 详情面板 `overflow-y: auto` + `position: fixed; inset: 0` 实现滚动
- `height: auto` 在 fixed 定位中与 `inset: 0` 冲突，去掉后滚动正常
- 背景点击关闭因 `max-width` 去掉后 `.project-detail` 撑满导致失效，通过 `e.target.classList.contains('project-detail')` 判断 padding 区点击修复
