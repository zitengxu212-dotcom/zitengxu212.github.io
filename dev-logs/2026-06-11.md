# 开发日志 — 2026-06-11

## 完成事项
- GSAP 优雅降级：main.js 全量包裹 `hasGSAP` 检测，所有动画（preloader、marquee、detail panel、stamp、text reveal、footer）均提供 CSS fallback，GSAP 不可用时页面仍可完整浏览
- GSAP CDN 从 cdnjs.cloudflare 切换到 jsdelivr（国内访问更友好）
- main.js 改用 `document.readyState` 判断 DOM 就绪，而非仅绑定 `DOMContentLoaded`
- build-fanke.py 多项改进：
  - `minify_js()` 修复 `//` 注释剥离误伤 `https://` 的问题
  - 新增 `_optimize_html()` 无损压缩（nbsp→字面空格、移除冗余 onclick/`<picture>` 包装、SVG 小数四舍五入）
  - 详情面板 JS（XZT 命名空间 + 事件监听）从代码块1拆至代码块2
  - `.t-card-featured-image-wrapper` CSS 合并入 `.t-card-featured-image`
- 文件夹重命名：`主页图片/` → `images/`（解决 GitHub Pages Linux 服务器上中文路径 404）
  - 涉及 54 个图片文件 + 6 个引用文件（index.html、main.js、styles.css、execution-steps.md、compress-images.py、2026-06-09.md）
- 配置 git 代理（127.0.0.1:7890）并推送至 GitHub
- 同步远程 `main` 分支（原仅 `master` 有新内容）
- **字体 Web 化部署修复**：
  - 根因：`@font-face` 仅使用 `src: local()`，其他人设备未装 Desolator 无法显示
  - 从 `AppData\Local\Microsoft\Windows\Fonts` 提取 `Desolator.otf` + `Desolator-Light.otf`
  - 使用 fonttools + brotli 转换为 WOFF2（74KB→29KB, 103KB→34KB, -61%/67%）
  - `styles.css` `@font-face` 新增 400/300 两档字重，各含 `local()` + `url('fonts/...woff2')`
  - `docs/design-spec.md` 补充字体文件路径和加载策略说明
- 更新开发日志（本文档）

## 待办事项
- 验证 GitHub Pages 部署后图片正常显示
- 验证 GitHub Pages 部署后字体正常显示
- 考虑删除远程冗余 `master` 分支

## 备注
- GitHub 远程仓库存在 `master` 和 `main` 双分支，`main` 为默认分支（GitHub Pages 部署源），已 force push 同步
- 本次两次 commit：`59f786e`（GSAP + build）+ `1b4e420`（rename）
