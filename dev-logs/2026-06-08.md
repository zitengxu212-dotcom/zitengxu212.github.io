# 开发日志 — 2026-06-08

## 完成事项

### 1. 修复轮播无限循环视觉跳变 Bug
- **根因**：`halfWidth` 基于 DOM 卡片数（8 → `cardWidth × 4`）而非实际独特内容数。set1 有 6 张独特卡片需 `cardWidth × 6`，差 2 张卡片宽度导致回绕时内容对不上
- **修复**（main.js `initWorks`）：
  - `halfWidth = uniqueCount × cardWidth`，回绕距离 = 一套完整独特卡片宽度
  - `renderCards` 内重算 `cardWidth`（实时 `offsetWidth + 40`）和 `halfWidth`
  - 立即 `gsap.set(track, { x: 0 })` + `lastTime = 0`，不等下一帧
  - 保持 8 张固定 DOM 卡片，set1 减至 4 张代表性卡片
- 提交 `f38b244`

### 2. GSAP CDN 迁移 + 凡科构建脚本修复
- **GSAP CDN 从 `<head>` 移至 `<body>`**：确保 `build-fanke.py` 能包含 CDN 脚本（构建脚本只提取 body 内容）
- **构建脚本重构**（tools/build-fanke.py）：
  - 修复 hero regex：`<section class="hero">` → `<section class="hero"[^>]*>`（适配 `id="hero"` 属性）
  - 拆分策略从「Hero / 其余」改为「**主页面 / 详情面板**」
  - 新增 `minify_html_aggressive()` — 详情面板 HTML 激进压缩，节省 ~8KB
  - 新增 `extract_detail_css()` — 代码块 2 只含精简 CSS（变量 + reset + 面板样式），节省 ~4KB
  - 代码块 2 只放静态 HTML + 精简 CSS，JS 全部在代码块 1
- 提交 `5ca55aa`

### 3. 删除 Brand 分类项目 09-12
- 移除 `projects` 注册表中 09-12 条目，03 补为 representative
- 移除 `_galleryImages` 中 09-12 配置
- 移除 `cardSets.set4`
- 移除 index.html 中 `#detail-09` ~ `#detail-12` 四个 article
- 现在 8 个项目（01-08），3 个分类，与 3 个标签按钮完全对应
- 提交 `8d5bd09`

### 4. 文档更新
- 更新 `docs/fanke-deploy-guide.md` 拆分策略图示和字符数追踪表
- 新增 `dev-logs/2026-06-08.md`

## 构建结果

| 代码块 | 内容 | 字符数 | 剩余 |
|--------|------|--------|------|
| 代码块 1 | 主页面 + 完整 CSS/JS + GSAP CDN | 47,142 | 2,858 |
| 代码块 2 | 8 个详情面板 + 精简 CSS | 35,360 | 14,640 |

## 项目当前规模

| 分类 | 标签 | 项目 |
|------|------|------|
| Representative (set1) | 4 张 | 01, 03, 05, 08 |
| Spatial (set2) | 4 张 | 01, 02, 03, 04 |
| Graphic (set3) | 4 张 | 05, 06, 07, 08 |

## 待办事项
- 替换卡片占位图为真实作品图片
- 填写详情页真实文字内容和画廊图片 URL
- 恢复 Awards 奖项区域
- Desolator 字体文件获取与部署
- 凡科实际上线测试

## 备注
- `halfWidth` 核心约束：必须等于 `uniqueCount × cardWidth`，且 DOM 卡片数 ≥ `2 × uniqueCount`（当前 8 = 4×2 ✅）
- 轮播用 rAF 自驱动（非 GSAP tween），避免页面切后台时 `killable-tween` 位置漂移
- 凡科代码块中 GSAP 通过 CDN 加载，全局可用，操作跨代码块 DOM 元素无问题
